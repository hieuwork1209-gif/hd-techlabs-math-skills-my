# Structural revision audit v4 — problem123

## Candidate identity

- **Branch:** `adversary/problem123`
- **Problem path:** `workspace/rainier-problem/problem123-commutative-algebra/problem.md`
- **Problem blob:** `4870bdf661ec4e0507dc8b464ff04fef2fdfcf91`
- **Solution blob:** `77a051032b768b803e3f91cc617057ad63ac6807`
- **Domain/Sub-domain:** Abstract Algebra -> Commutative algebra.
- **Current slot evidence:** 2026-09-11 taxonomy snapshot lists Commutative algebra with 1 remaining slot and status open.
- **Problem Type:** Symbolic derivation.
- **Answer Type:** Exact symbolic expression.
- **Final answer:** `\left\lceil\frac{2(n-1)}r\right\rceil-3`.
- **Stripped final-answer length:** 39 characters.

## Why this is a genuine structural revision

The solved predecessor used a two-variable endpoint generator set. Its graded exponent sets were one ordered family of equal-width intervals, so GPT-5.5 reduced everything to one adjacency inequality. The revised object is the natural three-variable degree-$n$ Veronese subalgebra generated by lattice points within distance $r$ of the three coordinate vertices. Degree membership is now equivalent to a three-coordinate capacity constraint; saturation requires an extremal residue argument, and sharpness of the conductor-power threshold requires a separate slack-allocation witness. This changes the dependency graph rather than increasing arithmetic or notation.

## Difficulty architecture

- **Natural object:** the conductor depth of a vertex-neighborhood monomial subalgebra inside the full three-variable Veronese algebra.
- **Hidden gateway:** write each generator as `(n-r)e_i + w` and discover the membership criterion
  $$
  \sum_{i=1}^3\left\lfloor\frac{p_i}{n-r}\right\rfloor\ge d.
  $$
- **Serial nodes:** (1) unique dominant-vertex decomposition; (2) converse reconstruction and capacity criterion; (3) extremal residue bound for the first full Veronese degree; (4) upper conductor-power inclusion; (5) lower sharpness by constructing a support vector with exact capacity; (6) allocate a degree-one multiplier inside the three coordinate slacks so no capacity jump occurs.
- **Closure:** the upper inclusion proves the candidate power works, while the explicit lower-degree monomial and multiplier prove one smaller power fails.
- **Decisive skeleton:** `vertex-capacity -> residue-saturation -> slack-witness`.
- **Workspace reuse:** 0 in the current mechanism registry; repository searches for the distinctive Veronese/conductor formulation returned no match on `main`.

## Triviality Probe

- **P1 State-space count:** no finite enumeration; the answer is a symbolic law for unbounded integers `n,r`, and the proof characterizes all graded lattice points at once -> PASS.
- **P2 Decoration-deletion:** full Veronese overring `B` -> Yes; three-variable vertex-neighborhood cutoff `max{i,j,l}>=n-r` -> Yes; hypothesis `n>=2r+2` -> Yes; standard grading -> Yes; requested irrelevant-ideal power condition -> Yes. Every component changes the algebra, the conductor threshold, or the meaning of the requested power -> PASS.
- **P3 Answer-triviality:** `ceil(2(n-1)/r)-3`; parameter-dependent, nonzero under the stated range, not degenerate -> PASS.
- **P4 Core-reduces-to:** decisive step is discovering the coordinate-capacity criterion and then proving its sharp saturation by residue capacity; no single named theorem supplies the route. Skeleton=`vertex-capacity-residue-saturation-slack-witness`; workspace reuse=0 -> PASS.
- **P5 Answer-recoverability:** small instances can be enumerated for verification, but no finite computation or numerical recognition recovers the exact law for arbitrary unbounded `n,r`; the answer is a general integer-valued formula -> PASS. Small-input/small-answer flag: clear because inputs are free parameters and output is not a fixed tiny value.
- **P6 Route-Concession:** 6a capacity criterion, saturation degree, and slack witness are all CONSTRUCT and are absent from the statement; 6b longest step conceded? No; 6c compaction-forced definition? No -> PASS.
- **P7 Depth-vs-Breadth:** 7a six serial nodes, not parallel cases; 7b at most three coordinate capacities are tracked simultaneously and only through one summed invariant; 7c each node is load-bearing -> PASS.
- **P8 Terminology-density:** N=0 bespoke terms; the prompt uses standard `Veronese algebra`, `standard graded`, and `irrelevant ideal` vocabulary. Max simultaneous primary definitions=2 (`A,B`); no renamed standard concept -> PASS.

## Reviewer completeness / correctness

- The generator decomposition is proved from `2(n-r)>n`, so the dominant coordinate is unique.
- The converse direction of the capacity criterion is proved by explicitly partitioning a residual vector of total `dr` into `d` vectors of total `r`.
- Both directions of the saturation threshold are proved: an upper bound on total exponent when the floor sum is at most `d-1`, and an explicit residue construction when `dr<=2(n-r)-3`.
- The equivalence between `A_+^m B subset A` and containment in the conductor is stated explicitly.
- The upper conductor inclusion uses the exact first saturation degree.
- Sharpness constructs a degree `D-2` monomial in `A` and then a degree-one Veronese multiplier by checking that the total coordinate slack is at least `n`; no cancellation argument is needed because the witness is a monomial.
- Exhaustive exact checks on a range of small admissible `(n,r)` pairs matched both the saturation degree and the least conductor-power formula. The proof itself is symbolic and characteristic-free.

## Promotion state

This is the one permitted solver-driven structural revision of the current commutative-algebra blueprint. It must receive exactly one GPT-5.5 Medium cold solve. If that exact revised blob is solved correctly, retire this blueprint and generate a fresh one; do not harden it again.
