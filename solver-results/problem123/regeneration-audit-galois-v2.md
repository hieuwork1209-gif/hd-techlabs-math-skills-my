# Regeneration audit — Galois structural revision v2 — problem123

## Selection

- **Domain/Sub-domain:** Abstract Algebra -> Galois theory.
- **Current slot evidence:** 2026-09-11 taxonomy snapshot lists Galois theory with 1 remaining slot and status open.
- **Problem Type:** Symbolic derivation.
- **Answer Type:** Exact symbolic expression.
- **Natural object:** the splitting field of the `n`th preimage equation for a point already lying `q` steps down the forward orbit of `x^2-2`.
- **Hidden gateway:** lift `x^2-2` through `x=z+z^{-1}` to the squaring map, then recognize that forward depth and backward depth overlap rather than simply producing the full radical tower.
- **Serial nodes:** (1) semiconjugacy and exact root formula; (2) identify effective radical depth `d=max(n-q,0)`; (3) prove the visible roots generate the full radical-cyclotomic compositum; (4) separate radical and cyclotomic fields by ramification at `ell`; (5) identify the surviving cyclic radical subgroup; (6) compute the reduction action of the full cyclotomic unit group on that subgroup.
- **Closure certificate:** faithful action on the distinct root set proves equality with the full compositum, while ramification proves the required field intersection and hence the exact group order and splitting.
- **Final answer:** `C_{2^d}\rtimes_\rho(\mathbb Z/2^n\mathbb Z)^\times, d=\max(n-q,0), \rho(a)b=ab`.
- **Stripped answer length:** 89 characters.

## Structural-revision justification

The first Galois blob was solved because the target `ell+ell^{-1}` leaves the entire backward radical depth visible immediately after the semiconjugacy. The revision changes the dependency graph by placing the target at forward depth `q`: the solver must determine how much of the radical tower has already been consumed, prove that the cyclotomic level does not collapse with it, and compute the resulting reduction action. This is the one solver-driven structural revision allowed for this blueprint.

Technique skeleton: `Chebyshev-semiconjugacy -> orbit-depth-overlap -> ramification-separation -> reduced-affine-action`.
Repository searches for the distinctive iterate/Galois and semiconjugacy/squaring formulations returned no matching problem on main.

## Mandatory triviality probes

- **P1 PASS:** the problem asks for a symbolic group law for arbitrary `n>=1`, `q>=0`, and odd prime `ell`; there is no finite state space whose enumeration yields the general answer.
- **P2 PASS:** every structural component is load-bearing. The odd prime supplies ramification separation, `n` sets the cyclotomic/preimage level, `q` changes the surviving radical depth, and the forward-orbit target is what creates the regime interaction.
- **P3 PASS:** the answer is a nondegenerate parameter-dependent family of finite groups; it is not a zero/trivial gotcha, although the cyclic factor correctly becomes `C_1` when `q>=n`.
- **P4 SOFT WARNING:** `x=z+z^{-1}` is a recognizable semiconjugacy, but it is not the whole solution. After it, the solver must still discover the depth collapse, prove full-field generation, establish a ramification-based intersection, and determine the reduction action. Workspace reuse for the full revised skeleton = 0.
- **P5 PASS:** fixed small `(n,q,ell)` cases are computationally accessible, but no finite/numerical side channel recovers the exact family and action for arbitrary unbounded `n,q`; the answer is structural rather than a recognizable numeric constant.
- **P6 PASS:** the statement says only that the target lies at forward iterate depth `q`; it does not reveal the multiplicative coordinate, effective radical depth, field intersection, or semidirect-product action.
- **P7 PASS:** six serial load-bearing nodes; at most three simultaneous standard objects are needed at the field-compositum stage, and no broad case enumeration is required. The `q<n` / `q>=n` split is generated by one intrinsic depth parameter and is unified by `d=max(n-q,0)`.
- **P8 PASS:** the statement defines only the primary polynomial `f` and splitting field `K_{n,q}`; iterate notation and Galois group are standard. No bespoke terminology or helper-definition stack is introduced.

## Reviewer completeness / correctness

- The semiconjugacy identity is derived explicitly and iterated by induction.
- The forward target is evaluated exactly, and the complete root set is derived from the two reciprocal multiplicative branches.
- Root distinctness is proved using absolute values, so the faithful index action is justified.
- Both regimes are covered: when `q<n`, the surviving radical has degree `2^{n-q}`; when `q>=n`, the multiplicative radius is rational and the splitting field is purely cyclotomic.
- For a surviving radical layer, `X^{2^d}-ell` is Eisenstein, `ell` is shown totally ramified in the pure radical field, and the power-of-two cyclotomic field is shown unramified at odd `ell`; hence the intersection is exactly `Q`.
- The cyclic radical subgroup and cyclotomic complement are identified, and conjugation is computed explicitly as multiplication modulo `2^d`.
- The uniform formula interprets `C_{2^0}=C_1` as the trivial group, so no endpoint regime is omitted.

## Promotion state

The revised normalized statement blob requires exactly one GPT-5.5 Medium cold solve. If that revised blob is solved correctly, this blueprint must be retired rather than revised again.
