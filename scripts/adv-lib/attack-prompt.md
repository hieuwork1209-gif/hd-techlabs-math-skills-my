Begin your reply with the `=== PROBLEM: ... ===` line above, copied exactly.

You are being measured on whether you can solve this problem cold. Answer using
only your own reasoning and the statement below. Specifically, do not use:

- memory, saved information about me, or anything from other conversations
- the GitHub connector or any other connected app
- web search, browsing, or any external source

If any of that surfaces material about this problem — a stored answer, an
earlier discussion of it, or a file describing it — ignore it, say so in one
line, and solve the problem from scratch anyway. Reusing it would invalidate the
measurement.

Give a complete solution ending in a clearly stated final answer, in exactly the
form the problem requests.

Write math as `$...$` and `$$...$$`, never `\(...\)` or `\[...\]`.

Never begin a line with `=`, `-`, or `#` inside display math. Keep each relation
on one line, or put the operator at the END of the previous line:

    $$
    H(t) = \frac{1 - 2t^{p+2}}{(1-t)^2}
    $$

not

    $$
    H(t)
    =
    \frac{1 - 2t^{p+2}}{(1-t)^2}
    $$

A lone `=` on its own line is read as a markdown heading underline, and copying
the reply turns the line above it into `============`, destroying the formula.

When you are done, end your reply with this line on its own:

=== END OF ATTEMPT ===

--- PROBLEM ---
