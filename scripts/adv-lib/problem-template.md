
--- REQUIRED problem.md SKELETON ---

`problem.md` must follow this skeleton exactly. The headings and the two-column
classification table are parsed by tooling, so a different shape breaks the
pipeline even when the mathematics is fine. Copy the structure verbatim and fill
only the angle-bracket parts.

# Normalized Math Problem

## LaTeX (Normalized)

<the full problem statement, and nothing else — no title, no metadata, no hints>

---

## Domain Classification

| Field | Value |
|---|---|
| **Domain** | <one of the portal's 13 domains> |
| **Sub-domain** | <sub-domain> |
| **Problem Type** | <one of the portal's problem types> |
| **Answer Type** | <one of the portal's answer types> |

---

## Domain Explanation

This problem involves <key mathematical elements>,
which are part of <chosen domain and sub-domain>.
The problem also involves <additional elements>,
which are part of <the next best domain and sub-domain>.
However, <why those are not core to the problem, or are less advanced>.

Specifically:

- The first heading is `# Normalized Math Problem` — not `# Problem`, not
  `# Problem <number>`.
- The statement lives under `## LaTeX (Normalized)`. Nothing may precede that
  heading except the title.
- The classification table has exactly two columns, `| Field | Value |`, with one
  row per field and the field name in `**bold**`. A four-column table with the
  fields as headers is wrong.
- `## Domain Explanation` is required and must use the five-part structure above.
